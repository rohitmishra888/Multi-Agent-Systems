# Crawler Tool
Step 2 of Phase 1 discovery pipeline.
HTML and JavaScript link crawling using BeautifulSoup4 + lxml.
Extracts API endpoint patterns from: <a href>, <form action>, fetch(), axios() calls.
