# Auth Detector + Classifier Tools
auth_detector.py: Detects JWT Bearer, API-Key, Basic Auth, OAuth2 requirements per endpoint.
classifier.py: Assigns functional category (auth, user-mgmt, data, admin) and initial risk level.
