# Multi-Agent API Security Testing Platform
# Phase 1: API Discovery Specialist
# Phase 2: Security Testing Specialist (OWASP API Top 10)
# Requirements file
# ─────────────────────────────────────────────────────────────────────────────
# Install all dependencies:
#   pip install -r requirements.txt
#
# Python version: 3.11 preferred (3.9+ compatible)
# ─────────────────────────────────────────────────────────────────────────────

# ── Core HTTP & Networking ────────────────────────────────────────────────────
httpx>=0.27.0,<1.0.0          # Modern async-ready HTTP client
requests>=2.31.0,<3.0.0       # Fallback HTTP library (some tools use it)
urllib3>=2.0.0,<3.0.0         # Underlying urllib3 (pinned for security)

# ── HTML / Web Parsing ────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0,<5.0.0 # HTML parsing for the crawler
lxml>=5.0.0                   # Fast HTML/XML parser backend for BS4

# ── Data Validation & Serialisation ──────────────────────────────────────────
pydantic>=2.6.0,<3.0.0        # Data models & validation
pydantic-settings>=2.2.0,<3.0.0  # BaseSettings for env-driven config

# ── YAML Support ─────────────────────────────────────────────────────────────
PyYAML>=6.0.1,<7.0.0          # YAML serialisation for catalog.yaml

# ── Environment & Configuration ───────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0   # .env file loading (also used by pydantic-settings)

# ── AI / CrewAI ───────────────────────────────────────────────────────────────
crewai>=0.80.0,<1.0.0         # Multi-agent orchestration framework
crewai-tools>=0.14.0,<1.0.0   # CrewAI tool utilities

# ── OpenAPI / Swagger Parsing ─────────────────────────────────────────────────
openapi-spec-validator>=0.7.0,<1.0.0  # OpenAPI spec validation

# ── Phase 2: Security Testing ─────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0.0            # JWT decode/encode for auth testing
sqlparse>=0.5.0,<1.0.0         # SQL parsing utilities for injection analysis
pdfplumber>=0.11.0,<1.0.0      # PDF text extraction (for reading assignment specs)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0      # Coverage reporting
pytest-mock>=3.14.0,<4.0.0    # Mock helpers

# ── Code Quality (optional, for development) ─────────────────────────────────
# ruff>=0.4.0                  # Linter + formatter
# mypy>=1.10.0                 # Static type checking
